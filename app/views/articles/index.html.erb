<div class="header">
  <div>
  <h3><%= link_to "Wikinimous", articles_path %></h3>
    <span>
    <%= link_to "New Article", new_article_path %>
  </span>
</div>
</div>

<br>

<div>
  <% @articles.each do |article| %>
  <h2><%= link_to article.title, article_path(article) %></h2>
  <p><%= article.content %></p>
  <% end %>
</div>
